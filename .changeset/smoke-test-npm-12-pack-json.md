---
---

CI only: the release smoke test now reads the object-keyed `npm pack --json` output that npm 12 emits as well as the array that npm 11 and earlier printed. Nothing in the published package changes.
