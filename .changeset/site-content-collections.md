---
---

Site-only change: move landing-page content into Astro content collections and generate `llms.txt`/`llms-full.txt` via file endpoints. No change to the published `sync-worktrees` package, so no version bump.
