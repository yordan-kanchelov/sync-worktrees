---
"sync-worktrees": minor
---

Add published config autocomplete types for JavaScript config files. Generated and example configs now use `// @ts-check` plus JSDoc `@satisfies` annotations with zero runtime imports, and the package publishes `dist/index.d.ts` for editors to resolve `SyncWorktreesConfig`.
