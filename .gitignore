node_modules/
dist/
*.log
.DS_Store
.env
.env.local
coverage/
.nyc_output/
*.tsbuildinfo

# Local Codex CLI config
.codex/

# npm pack artifacts
*.tgz

# Changesets temp files
.changeset/_tmp.md
.changeset/pre.json

# User config files (but keep examples)
sync-worktrees.config.js
!demo-recording/sync-worktrees.config.js

# Demo recording artifacts
demo-recording/fixture/
demo-recording/.sync-worktrees-state/
demo-recording/*.cast
demo-recording/*.gif

# Astro site build artifacts
site/.astro/
site/public/demo.gif

# MCP stdio integration-test build dirs (bundled inside the repo so the spawned
# server resolves node_modules; removed in the test's finally block)
.mcp-stdio-test-*
