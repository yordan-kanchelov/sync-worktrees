---
question: "What's the difference between git worktree and sync-worktrees?"
order: 2
---

`git worktree` adds one extra folder. This makes every selected branch a folder. Those folders stay in sync from one config.
