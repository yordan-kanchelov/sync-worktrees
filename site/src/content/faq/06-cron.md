---
question: "Can I run it continuously or on a cron?"
order: 5
---

Leave the TUI open and it syncs on a schedule, hourly by default. Headless, put `--runOnce` on a cron job or a timer. Overlapping runs are skipped.
